{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/check-database-url.mjs && prisma migrate deploy && prisma generate && next build",
  "installCommand": "npm install"
}
